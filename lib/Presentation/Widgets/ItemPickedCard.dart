import 'package:flutter/material.dart';
class ItemPickedCard extends StatelessWidget {
 // const ItemPickedCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
